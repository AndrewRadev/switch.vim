let g:switch_definitions = [['a', 'b'], ['否', '是']]
" let b:switch_definitions = [['a', 'b'], ['否是', '是否']]

" a b
" 否是

" let b:switch_definitions = [
"       \ {'是.*是': {'是': '否'}},
"       \ {'否.*否': {'否否': '是是'}},
"       \ ]

" 否否-否否
" 是是-是是|
